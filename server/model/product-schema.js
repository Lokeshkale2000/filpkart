// product-schema.js
/*import mongoose from 'mongoose';

const productSchema = new mongoose.Schema({
    id: {
        type:String,
        required:true,
        unique:true,
    },
    url: String,
    detailUrl: String,
    title: Object,
    price: Object,
    quantity: Number,
    description: String, // Fixed typo from 'descripation' to 'description'
    discount: String,
    tagline: String, 
});

// Export the Product model using ES module syntax
const Product = mongoose.model('product', productSchema);
export default Product; // Change this to export the Product model */
